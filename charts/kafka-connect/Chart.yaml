apiVersion: v2
name: kafka-connect
description: Production-ready Kafka Connect chart with OTEL/JMX agent init containers, connector health monitoring sidecar, HPA, PDB, and configurable connectors
version: 1.0.0
type: application
appVersion: "7.7.0"
